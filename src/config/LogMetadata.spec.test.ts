/* tslint:disable:max-classes-per-file */
import * as _ from "lodash";
import "mocha";

describe("LogMetadata", () => {
    describe("getBestIdentifier", () => {
        it("Includes service in identifier if present");
    });
});
